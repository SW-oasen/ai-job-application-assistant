# Application Assistant Backend

FastAPI-Backend für Import, Validierung, Persistenz und weitere
Geschäftslogik. Sprint 1 stellt Konfiguration, strukturiertes Logging,
einheitliche Fehlerantworten und `GET /health` bereit.

